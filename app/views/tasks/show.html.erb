<ul>
    <li><%= @task.title %></li>
    <li><%= @task.details %></li>
    <li>Completed: <%= @task.completed %></li>
    <%= link_to "Back to home", tasks_path %>
    <%= link_to "Edit task", edit_task_path(@task) %>
    <%= link_to "Delete task", delete_task_path(@task), method: :delete, data: { confirm: "Delete #{@task.title}?" }%>
</ul>
